# prototype

Static, hardcoded-data design walkthrough for the file browser and category
tree, built before any backend exists — the same role `ui-prototype/` plays
in nutrilens. Nothing lives here yet.
