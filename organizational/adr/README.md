# organizational/adr

Architecture decision records — one file per significant, hard-to-reverse
choice (why `apps/vision` is a separate service, why the category tree is
`ltree` rather than a self-referencing table, why the vision model is
self-hosted rather than a cloud API). None written yet; this pass is
scaffolding only.
