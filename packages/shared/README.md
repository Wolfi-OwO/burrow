# packages/shared

TypeScript types and validation schemas shared between `apps/api` and
`apps/frontend` (category-tree shapes, file metadata, API request/response
types) so the two don't drift. Nothing lives here yet — no code, no
`package.json`.
